cache: --delete-- npm
language: node_js
branches:
  only:
    - master
    - /^greenkeeper/.*$/
    - /^mkpr/.*$/
    - /^npm-template-sync/.*$/
jobs:
  include:
    - stage: test
      node_js:
        - -12
        - -13
